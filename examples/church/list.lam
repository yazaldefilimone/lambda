(λcons nil add zero one two three.
  (λlist. list add zero) (cons one (cons two (cons three nil)))
)
(λh t c n. c h (t c n))
(λc n. n)
(λm n f x. m f (n f x))
(λf x. x)
(λf x. f x)
(λf x. f (f x))
(λf x. f (f (f x)))
